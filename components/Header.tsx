import NavBar from 'components/NavBar';

const Header = () => {
    return (
        <header className="bg-gradient-to-br from-skin-primary-start to-skin-primary-end h-[85vh]">
            <NavBar />
        </header>
    );
};

export default Header;
